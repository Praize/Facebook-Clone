name: Docker Build #Docker build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest #OS

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t facebook-clone .